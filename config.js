/**
 * 模块配置文件，包括目录名，源代码位置等等
 * @type {{MODULE_NAME: string, SOURCE_DIST: string, BUILD_DIST: string, RELEASE_DIST: string}}
 */
module.exports = {
  MODULE_NAME: 'payrolls',
  SOURCE_DIST: './app',
  BUILD_DIST: './dest',
  RELEASE_DIST: './output',
};
